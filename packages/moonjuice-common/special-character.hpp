#pragma once

namespace MoonJuice {
  enum class SpecialCharacter : uint8_t {
    OpenBracket,
    CloseBracket,

    OpenSquareBracket,
    CloseSquareBracket,

    OpenCurlyBracket,
    CloseCurlyBracket,

    Comma,
    Colon,
  };
}
